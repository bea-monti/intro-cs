void setup() {
  size (500, 500);
  
  for(int y2 = 5; y2 < width; y2 += 12) {
  circle (y2, 480, 10);
  }
   for(int y = 5; y < width; y += 12) {
  circle (y, 400, 10);
   }
  for(int y3 = 5; y3 < width; y3 += 12) {
  circle (y3, 460, 10);
  
  

  }
}
